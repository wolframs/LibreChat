{
  "$comment": [
    "The session runs in its own worktree. These deny rules and its briefing are",
    "workflow constraints, not a security sandbox against arbitrary shell code.",
    "The wrapper validates credential paths and the unchanged test gate before",
    "publication. It alone owns integration, deployment and worktree cleanup.",
    "deploy.sh remains editable for feature markers; running it is denied.",
    "A strict empty MCP configuration prevents recursive code-agent filings."
  ],
  "permissions": {
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(./librechat.yaml)",
      "Read(./searxng/settings.yml)",
      "Read(./data-node/**)",
      "Edit(./.env)",
      "Edit(./.env.*)",
      "Edit(./librechat.yaml)",
      "Edit(./searxng/settings.yml)",
      "Edit(./scripts/agent-test.sh)",
      "Edit(./CHANGELOG-agent.md)",
      "Bash(git push:*)",
      "Bash(git worktree:*)",
      "Bash(git checkout:*)",
      "Bash(git switch:*)",
      "Bash(git stash:*)",
      "Bash(docker:*)",
      "Bash(launchctl:*)",
      "Bash(./scripts/deploy.sh:*)",
      "Read(~/Projects/.forgejo-env)",
      "Read(~/.ssh/**)",
      "Read(~/.aws/**)",
      "Read(~/.config/gh/**)",
      "Bash(cat ~/Projects/.forgejo-env:*)",
      "Bash(cat ~/.ssh/*:*)"
    ]
  }
}
